export interface IParkCardProps {
  locale: string;
  name: string;
  carSpot: boolean;
  bikeSpot: boolean;
  truckSpot: boolean;
  rating: string;
  price: number;
  period: string;
  image?: string;
  id: string;
}
